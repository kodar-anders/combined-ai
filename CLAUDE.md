# CLAUDE.md

## Agent instructions (read first)

- Don't over-engineer. Concretely: no abstractions for hypothetical needs, no unrelated refactors while making a fix, prefer 2–3x inline duplication over a premature helper.
- **Never add `Co-Authored-By: Claude`** or any AI attribution trailer to commit messages.
- After editing any file, always run `yarn eslint <path>` on the changed file(s) and `yarn typecheck`. Also run
  prettier on the change file(s). Don't skip either, and don't wait until "done" — run them as part of the edit loop. If any of the edited files are covered by Jest tests, also run those specific tests (`yarn test path/to/file.test.ts`) before declaring the task done. Use `yarn eslint <path>` rather than the full-project `yarn lint`.
- Always update `CLAUDE.md` whenever you make any change that might affect the agent's behavior, or when you make a
  change that you think should be documented. Basically, try to always keep `CLAUDE.md` up-to-date with the project.
- Keep `CLAUDE.md` compact. It's instructions, not documentation — record only what an agent can't infer from the code (commands, conventions, gotchas, non-obvious decisions), and point to the file rather than restating its types/structure. Prefer bullets over paragraphs. When you add a line, delete or compress a stale one; if a line could be removed without causing a mistake, remove it. If a section is growing past a few tight bullets, that's a signal to compress, not to keep appending.
- Keep `README.md` and `CHANGELOG.md` up to date. For the readme file: record notable changes under the `## 
[Unreleased]` section (Keep a Changelog format). `README.md` and `CHANGELOG.md` exist at the project root.
- Don't run the integration tests unless explicitly asked — they make live, **paid** API calls.
- Personal / per-developer instructions (working cadence, local-only artifacts) live in a gitignored `CLAUDE.local.md` at the project root, which loads automatically after this file. Keep this committed `CLAUDE.md` to project-wide facts.

## What this is

A **plain TypeScript library** meant to be imported by other projects. Its purpose is to **combine several AI services/providers into one package** — selecting a single provider for a prompt, and (later) combining multiple providers on one prompt. `src/index.ts` is the public entry point — re-export the package's public API from there. The foundations (build, test, lint, type-check) are in place and three providers (Anthropic, OpenAI, Gemini) are implemented.

### Library structure (current)

- `src/types.ts` — the provider-agnostic contract every provider implements: `Message`, `CompletionRequest`, `CompletionResult`, and `Provider` (`complete()` → full text, `stream()` → `AsyncIterable<string>` of text deltas).
- `src/providers/anthropic.ts` — `AnthropicProvider`, talking to the Anthropic Messages API (`POST /v1/messages`) directly over the global `fetch` — **no SDK dependency**. Default model `claude-opus-4-8`. Streaming parses the SSE body and yields `content_block_delta` → `text_delta` text. The request body is kept minimal (no `temperature`/`top_p`/`top_k`/`thinking` — Opus 4.x rejects sampling params).
- `src/providers/openai.ts` — `OpenAIProvider`, talking to the OpenAI Chat Completions API (`POST /v1/chat/completions`) directly over `fetch` — **no SDK dependency**. Default model `gpt-4.1`. Auth via `Authorization: Bearer`. The optional `system` prompt is folded into a leading `{ role: "system" }` message (OpenAI has no top-level system field), and the token cap is sent as `max_completion_tokens` (works on the o-series too). Streaming parses SSE, yields `choices[0].delta.content`, and stops on the `data: [DONE]` sentinel.
- `src/providers/gemini.ts` — `GeminiProvider`, talking to the Google Generative Language API (`POST /v1beta/models/{model}:generateContent`, streaming via `:streamGenerateContent?alt=sse`) directly over `fetch` — **no SDK dependency**. Default model `gemini-2.5-pro`. Auth via the `x-goog-api-key` header. Non-obvious API shape: model + action live in the URL path (not the body); `assistant` role maps to `model`; the optional `system` prompt is a top-level `systemInstruction`; the token cap is `generationConfig.maxOutputTokens`. **Thinking-token gotcha:** Gemini 2.5 are thinking models whose thinking tokens count against `maxOutputTokens` (unlike Anthropic/OpenAI, where `maxTokens` budgets only visible output), so a tiny cap can return empty/truncated text. We don't disable thinking (the default model can't fully disable it) — give Gemini a generous cap.
- `src/registry.ts` — `ProviderRegistry`, the package's **single point of access** to its providers. The provider classes are **not exported**; the registry constructs the built-ins by name from a config object. Constructor takes `ProviderRegistryConfig` (`{ anthropic?: AnthropicProviderOptions; openai?: OpenAIProviderOptions; gemini?: GeminiProviderOptions }`) and constructs only the providers present. `select(name)` is typed to `ProviderName` (`"anthropic" | "openai" | "gemini"`) and returns the `Provider` or throws an `Error` listing the configured names; plus `has(name: string)` and `names()` (fixed order: anthropic, openai, gemini). It never reads env vars — keys come from the config. Also hosts `combine(request)` (see `src/combine/`): it validates the request (`messages` non-empty, `strategy` against the runtime `STRATEGY_NAMES`, `minParticipants` an integer in `[1, participants]`, synthesizer membership), resolves participant names → a roster of `{name, provider}` (each via `select()`), defaults the synthesizer to the first participant, and dispatches the strategy (one today, so it calls `consensus` directly — at ≥2 strategies this becomes a `switch (request.strategy ?? "consensus")`).
- `src/combine/` — the **combine** feature: make multiple providers cooperate on one prompt via a selectable strategy. **One file per strategy** (so `combine` doesn't grow into one huge file). Public entry is `ProviderRegistry.combine(request)`.
  - `src/combine/index.ts` — the public barrel: the combine types (`StrategyName`, `CombineRequest`, `ParticipantOutcome`, `CombineResult`, `CombineEvent`, `CombineOptions`) plus the one runtime value, `STRATEGY_NAMES` (the source of truth `combine()` validates against). It imports `ProviderName` from `../registry` as a **`type`**, so the combine↔registry cycle stays type-only/erasable — keep it that way (no value imports from `../registry`).
  - `src/combine/consensus.ts` — the **consensus** strategy and all its private helpers. The internal `consensus(roster, synthesizer, request, onEvent?)` is **not exported from the package**; `registry.ts` imports it from `"./combine/consensus"`. The synthesizer is passed **by name** (a `ProviderName`), not a positional index. Each phase is a shaped `system` + single `user` message to `complete()` — no provider-specific code, **no streaming for combine yet**. Three phases:
    - **Phase 1 (draft):** every participant drafts in parallel.
    - **Phase 2 (critique):** every survivor critiques all drafts (**anonymized** to `Answer A/B/C` by default; `attribution: "attributed"` shows provider names — the returned `ParticipantOutcome.provider` always carries names regardless), ending with a structured verdict (`BEST:` / `KEY FIX:` / `CONFIDENCE:`).
    - **Phase 3 (synthesize):** the synthesizer reads drafts + critiques and writes the final answer.
    - **Framing invariants — preserve if you touch the constants** (rationale in the `consensus.ts` doc-comments): `SYNTH_FRAMING` makes the synthesizer adjudicate on **correctness over popularity** and write a **self-contained answer that never alludes to the drafts/critiques/process or internal labels** (the model kept leaking `Answer A/B/C` / "the candidates" — don't reintroduce that). Because prompt-only proved unreliable, a **sanitizing second pass** (`SANITIZE_FRAMING` + `sanitizeAnswer()`) rewrites the synthesis to strip leftover process narration, falling back to the un-sanitized answer on empty/failure — this is **one extra `complete()` call** on the multi-provider path only. The draft/critique phases prepend `CONCISE_DIRECTIVE`, which strips ceremony **but keeps reasoning/assumptions/caveats** (the audience is other AIs that need the _why_) — don't reword it to suppress reasoning, and don't add it to the user-facing synthesis.
    - **Test coupling:** the test's `phaseOf` switches on literal markers in the framing constants — `"Rewrite the following"` (sanitize), `"lead assistant"` (synth), `"produce only a critique"` (critique). Keep each constant free of the others' markers.
    - **Events:** `combine()` accepts `CombineOptions` (`{ onEvent? }`) threaded to `consensus`, which emits `CombineEvent`s (`{type:"phase"}` per boundary; `{type:"draft"|"critique", provider, status}` as each call settles) via an `emit` that **swallows handler errors**. Status events only — no token streaming, no terminal event; the result is the returned `CombineResult`.
    - **Failure / survivor policy:** fan-outs use `Promise.all` over per-participant `runOutcome()` calls that capture success/failure as a `ParticipantOutcome`, so one provider failing never rejects the round. 0 drafts → throw; single-provider roster → plain completion; fewer than `minParticipants` (default 2) surviving drafts → throw; failed critiques are non-fatal (synthesize from drafts alone); a failed synthesizer falls back to the next survivor.
  - **Adding a strategy:** new `src/combine/<strategy>.ts` exporting its orchestrator, add a member to the `StrategyName` union in `index.ts`, and wire it into `registry.combine()` — at the second member the direct call becomes a `switch (request.strategy ?? "consensus")` (lint-clean once the union has ≥2 members). Helpers in `consensus.ts` stay private to it; **only promote a shared helper when a second strategy actually reuses it** (the 2–3x-duplication rule — don't pre-extract a shared module for hypothetical needs). `CombineResult`'s `drafts`/`critiques` shape is consensus-specific; revisit it (likely a discriminated union on `strategy`) when a second strategy with different artifacts lands, not before.
  - Tests in `src/combine/__tests__/`: `consensus.test.ts` uses network-free fake `Provider` objects (no `fetch` mock) that classify the phase from the system prompt and record calls (it covers the `consensus()` strategy only — the `ProviderRegistry.combine()` entry validation lives in `src/__tests__/registry.test.ts`, beside the code under test); the live `consensus.integration.test.ts` runs the real three-way consensus — **triple-gated** on `RUN_LIVE_TESTS=1` + all three keys (it needs ≥2 participants); each provider uses its cheap model via the registry config default (combine applies one `model` to all participants, so per-provider models can't be set per call) and `maxTokens` is generous for Gemini's thinking tokens. **`noUncheckedIndexedAccess` gotcha:** don't zip parallel arrays by index (`a[i]` alongside `b[i]`) — carry paired data in one structure (the roster) instead.
- New providers: add `src/providers/<name>.ts` implementing `Provider`, then wire it into `ProviderRegistry` (a new optional field on `ProviderRegistryConfig` + a construction branch). The provider class itself stays internal — only `index.ts` re-exports its **options type** and the registry handles construction. `index.ts` is the single public entry point; do not export provider classes.
- Tests live in a `__tests__/` folder **colocated** with the code under test — provider tests are in `src/providers/__tests__/` (import via `../<provider>`), combine tests are in `src/combine/__tests__/` (import the strategy via `../consensus`, and `src/`-level code via `../../registry` / `../../types`), and `src/`-level code like `registry.ts` is tested in `src/__tests__/` (import via `../registry`). The convention is one `__tests__/` per source folder, as close to the files under test as possible. Each provider has `<provider>.test.ts` (mocked unit tests) and `<provider>.integration.test.ts` (live, opt-in). Keep the `.test.ts` / `.integration.test.ts` suffix (the latter so the `test:integration` pattern filter matches them).

## Commands

- `yarn build` — bundle `src/index.ts` to `dist/` via **tsup** as dual ESM (`index.js`) + CJS (`index.cjs`) with `.d.ts`/`.d.cts` declarations and sourcemaps.
- `yarn typecheck` — `tsc --noEmit`. tsc is type-check only; tsup owns the build.
- `yarn test` — Jest. Single test: `yarn test <pattern>` or `yarn test -t "<name>"`. Watch: `yarn test:watch`.
- `yarn test:integration` — **live** API tests (`*.integration.test.ts`). Double-gated per provider: a suite runs only when both `RUN_LIVE_TESTS=1` (the script sets it) and that provider's key are present (`ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `GEMINI_API_KEY`); otherwise it's `describe.skip`, so plain `yarn test` always skips it and never makes a paid call. Live tests use the cheap model per provider (`claude-haiku-4-5` / `gpt-4.1-mini` / `gemini-2.5-flash`) with a small `maxTokens` to keep cost negligible — single-call suites use ~16 (Gemini 512, to leave room for its thinking tokens), the combine suite uses 2048. The script defaults to all integration tests; append a Jest filename pattern to narrow to one — `yarn test:integration openai.integration` (the pattern **replaces** the `integration` default).
  - **Env loading:** `jest.setup.cjs` (wired via `setupFiles` in `jest.config.mjs`) loads a gitignored `.env` via `dotenv` so the key is picked up automatically. Copy `.env.example` → `.env` and paste your `ANTHROPIC_API_KEY` / `OPENAI_API_KEY` / `GEMINI_API_KEY`. **Only the tests read env** — the library itself never reads env vars; consumers always pass `apiKey` to the provider explicitly.
- `yarn lint` / `yarn lint:fix` — ESLint.
- `yarn format` / `yarn format:check` — Prettier.

## Architecture / toolchain choices

- **Build = tsup, type-check = tsc.** tsconfig has `noEmit: true`; it exists for editor IntelliSense and `yarn typecheck` only. The build settings live in the `tsup` flags in `package.json`'s `build` script.
- **Tests run through `@swc/jest`** (`jest.config.mjs`), which transpiles TS→CJS fast and does _no_ type-checking — types are covered separately by `yarn typecheck`. Test files import globals explicitly from `@jest/globals` (e.g. `import { describe, it, expect } from "@jest/globals"`) rather than relying on ambient types.
- **Dual-format packaging** is wired in `package.json` via the `exports` map (`import`→`.js`/`.d.ts`, `require`→`.cjs`/`.d.cts`). `files: ["dist"]` and `prepack: yarn build` mean the build is produced on publish/pack.
- **Node globals are typed via `@types/node`** (devDependency), referenced explicitly with `"types": ["node"]` in `tsconfig.json`. This is what makes `fetch`, `Response`, `ReadableStream`, `TextDecoder`, etc. resolve under the ES2022-only `lib` — the provider transport relies on them.

## Package management — Yarn 4 PnP

Uses **Yarn 4 (Berry) with Plug'n'Play** — there is no `node_modules`; deps resolve via `.pnp.cjs` / `.pnp.loader.mjs`. Always use `yarn` (never `npm`).

- **PnP strictness gotcha:** you can only import packages declared directly in `package.json`. A transitive-only dep won't resolve (this is why `@jest/globals` is an explicit devDependency).
- `@jest/globals` versions lag `jest`'s — they are not the same number. Match the version Jest actually pulls when bumping.
- `tsconfig` sets `ignoreDeprecations: "6.0"` solely because tsup's `.d.ts` bundler injects a now-deprecated `baseUrl` under TypeScript 6.

## Lint conventions (enforced)

`eslint.config.mjs` is a flat config: type-aware (`strictTypeChecked` + `stylisticTypeChecked`) + unicorn + promise for `**/*.ts`; Jest rules scoped to test files only; a non-type-checked block for `*.{js,cjs,mjs}`. Notable rules:

- **`type` over `interface`**; **`import type`** (inline style) for type-only imports; `consistent-type-exports`.
- Exported API must declare types (`explicit-module-boundary-types`) and functions need return types (`explicit-function-return-type`, callbacks exempt).
- `no-console: error` (a library shouldn't log to a consumer's console) — off in tests.
- Prefix intentionally-unused args with `_`; unused imports are an error.
- Single-line `case` clauses omit braces. Prettier owns formatting (ESLint defers via `eslint-config-prettier`).

## Notes

- `"private": true` in `package.json` — flip to publish to a registry (the packaging fields already work for git/workspace installs).
